📘 C++ Concepts – Theory Notes

1. Introduction to C++

Developed by Bjarne Stroustrup at Bell Labs (1983).

Extension of C language with OOP features.

Supports low-level programming + high-level abstraction.

2. Basic Concepts

Tokens → Keywords, Identifiers, Literals, Operators, Separators.

Data Types → Primitive (int, float), Derived (array, pointer), User-defined (struct, class).

Input/Output → cin, cout (from <iostream>).

3. Control Structures

Decision Making → if, if-else, switch.

Loops → for, while, do-while.

Jump → break, continue, return, goto.

4. Functions

Modular code → reduces redundancy.

Types: Normal, Inline, Recursive.

Overloading → Same function name with different parameters.

Default Arguments → Values passed if not specified.

5. OOP Concepts

Class & Object → Blueprint & instance.

Encapsulation → Data hiding (private, public, protected).

Inheritance → Reusability (Single, Multiple, Multilevel, Hybrid).

Polymorphism → One name, many forms (Compile-time & Run-time).

Abstraction → Hiding implementation, showing only features.

Friend Function → Access private members.

6. Constructors & Destructors

Constructor → Initializes object.

Types: Default, Parameterized, Copy.

Destructor → Cleans up (starts with ~).

Overloading allowed (except destructor).

7. Inheritance in Detail

Modes: Public, Private, Protected.

Virtual Base Class → Solves diamond problem.

8. Polymorphism

Compile-time → Function overloading, Operator overloading.

Run-time → Virtual functions.

Pure virtual function → Makes class abstract.

VTable & VPtr → Mechanism behind runtime polymorphism.

9. Memory Management

Pointers → Variables storing addresses.

Dynamic memory → new, delete.

Smart Pointers → unique_ptr, shared_ptr, weak_ptr.

Prevents memory leaks & dangling pointers.

10. Templates

Function Template → Generic functions.

Class Template → Generic classes.

Template Specialization → Custom behavior for specific types.

11. Exception Handling

try – throw – catch mechanism.

Handles runtime errors safely.

Supports multiple catch blocks.

Can define custom exceptions.

12. File Handling

Classes: ifstream, ofstream, fstream.

Modes: in, out, app, trunc, binary.

Supports read, write, append.

13. Advanced Concepts

Namespace → Avoids name conflicts.

Type Casting → static_cast, dynamic_cast, const_cast, reinterpret_cast.

Lambda Expressions → Inline anonymous functions.

Move Semantics → Improves efficiency with rvalue references.

14. Standard Template Library (STL)

Containers: vector, list, map, set, stack, queue.

Iterators: Access elements sequentially.

Algorithms: sort, search, etc.

Functors: Function objects.

15. Multithreading

Thread → Smallest unit of execution.

Thread Library → <thread>.

Synchronization → Mutex, Locks, Condition variables.

Issues: Race condition, Deadlock.
